import { MapDirectionDirective } from './map-direction.directive';

describe('MapDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MapDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
